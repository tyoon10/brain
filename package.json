{
  "name": "brain-roundtable",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BRAIN Roundtable — local-first sittings. Preview site for github.com/tyoon10/brain.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "eleventy --serve --port 4321",
    "build": "rm -rf _site && eleventy",
    "validate": "node scripts/validate.mjs",
    "lint:copy": "node scripts/lint-copy.mjs",
    "test": "node --test tests/*.test.mjs",
    "assert:html": "node scripts/assert-no-client-js.mjs",
    "assert:firewall": "node scripts/assert-firewall.mjs",
    "assert:pages": "node scripts/assert-pages.mjs",
    "check:links": "node scripts/check-outbound-links.mjs",
    "ci": "npm run validate && npm run lint:copy && npm test && npm run assert:firewall && npm run build && npm run lint:copy -- --built && npm run assert:html && npm run assert:pages && npm run assert:firewall"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "yaml": "^2.9.0",
    "zod": "^4.5.4"
  }
}
